// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        mavenCentral()
        google()
        maven { url "https://jitpack.io" }
        // 替换失效的 jcenter，使用阿里云和 Bintray 镜像源
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        maven { 
            url 'https://jcenter.bintray.com'
            allowInsecureProtocol = true
        }
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.1.1"
        classpath 'com.jakewharton:butterknife-gradle-plugin:10.1.0'//黄油刀

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        
        // 1. 阿里云镜像源 (解决大部分第三方依赖和旧版 JCenter 缺失问题)
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        
        // 2. JCenter 备用节点 (解决 com.sunmi 商米 SDK 下载)
        maven { 
            url 'https://jcenter.bintray.com'
            allowInsecureProtocol = true
        }
        
        // 3. 个推 SDK 仓库 (已修复 https 及 allowInsecureProtocol)
        maven {
            url "https://mvn.gt.getui.com/nexus/content/repositories/releases/"
            allowInsecureProtocol = true
        }
        
        // 4. 蒲公英 SDK 仓库
        maven { url "https://raw.githubusercontent.com/Pgyer/mvn_repo_pgyer/master" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
